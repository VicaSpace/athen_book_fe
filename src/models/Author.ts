export default interface Author {
    id: number
    name: string,
    img: string,
    createdAt: string,
    updatedAt: string,
    bornOn: string,
    bornIn: string,
    children: number,
    spouse: string
}